language: ruby
rvm:
  - 2.3
  - 2.4
  - 2.5
  - 2.6
  - ruby-head
  - jruby
script:
  - bundle exec rake test
  - bundle exec rake rubocop
matrix:
  allow_failures:
  - rvm: jruby
  - rvm: ruby-head
  fast_finish: true
