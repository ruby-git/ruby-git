language: ruby
rvm:
  - 2.3
  - 2.4
  - 2.5
  - ruby-head

matrix:
  include:
    - rvm: jruby
      env: JRUBY_OPTS="--server -Dcext.enabled=true -Xcompile.invokedynamic=false"
  allow_failures:
  - rvm: jruby
  - rvm: ruby-head
  fast_finish: true